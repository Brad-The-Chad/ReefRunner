function love.conf(t)
    t.title = "Reef Runner"  -- The window title (string)
    t.console = true -- Attach a console (boolean, Windows only)
    
    
    t.window.icon = "sprites/SharkLogo.png"
    
end